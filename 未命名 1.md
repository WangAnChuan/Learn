### 第三章 方法论

#### 3.1 整体框架

针对水面漂浮物检测中存在的背景动态干扰（如光照反光、连续波纹）以及特定单一类别（如透明塑料瓶）视觉特征模糊的问题，本文提出了一种物理感知与指标驱动的目标检测框架（Physically-Informed and Metric-Driven Framework）。

[这里插入图 3：YOLO11-MCN 整体网络架构图，需清晰标出多模态输入端与 C3k2-Mamba 模块的位置]

如图 3 所示，该框架以 YOLO11 为基准架构，并在两个关键维度上进行了创新性重构：

- **物理感知层面（Data-Centric）**：本框架摒弃了传统的单模态输入，转而采用异构多模态联合训练策略。除了光强模态（RGB 与 Mono），进一步引入了包含线偏振度（DoLP）、偏振角（AoP）、Pauli 分解及差分（DIF）在内的全偏振物理特征。这种高维物理信息不仅补充了单一目标表面的几何与材质纹理，更促使模型通过多流架构学习跨模态的光照不变性特征，从物理源头增强了模型在强反射或低对比度场景下对特定透明目标的鲁棒性。
    
- **指标驱动层面（Model-Centric）**：不同于经验性的网络堆叠，本研究提出了一种指标驱动的架构优化策略。研究利用层级特征聚类评估（FCE）作为诊断探针，量化特征空间的流形分布。诊断发现特征融合阶段（特别是 Layer 19）存在显著的**目标类内方差漂移（High Intra-class Variance of the Target）**，表明模型未能有效捕捉全局上下文以维持单一类别的语义一致性。以此为据，本文将状态空间模型（Mamba）嵌入该瓶颈层级，利用其线性复杂度的全局感受野压缩同类目标特征分布，大幅提升了特征的语义紧致度（Semantic Compactness）与判别力。最终构建的模型命名为 **YOLO11-MCN**。
    

#### 3.2 物理感知的异构数据策略

传统的水面目标检测算法过度依赖可见光图像，极易受到镜面反射（Specular Reflection）与视觉伪装的影响。为突破此物理瓶颈，本文并未盲目扩展网络通道，而是提出了一种基于物理域随机化（Physical Domain Randomization）的异构数据增强策略。

具体而言，原始传感器数据被解译为 K=6 种异构物理视图，作为独立训练样本混入数据流：除了提供基础纹理的强度模态外，重点引入了偏振模态。由于水面反光呈现高偏振态，而目标漫反射偏振度较低，这些偏振模态为前景分离提供了正交的物理线索。

形式化地，构建异构数据集 $D = \{ (I_{RGB}, y), (I_{DoLP}, y), \dots \}$，其中 $y$ 表示单类别 Bottle 的检测标注。在训练迭代中，同一 Bottle 目标的各类物理视图被随机采样作为输入，迫使主干网络剥离模态特定的高频干扰，转而聚焦学习目标实体在跨物理域下恒定不变的空间结构特征。

#### 3.3 指标驱动诊断：层级特征聚类评估

为降低深度神经网络设计中的不可解释性，本研究提出了层级特征聚类评估（Hierarchical Feature Clustering Evaluation, FCE）框架。针对本研究的单类别检测任务，FCE 旨在量化特征流形空间中唯一前景（Bottle）与背景（Water）的二元分布质量。

**3.3.1 FCE 评估机制**

令 $F_l$ 表示第 $l$ 层的特征张量。本研究通过 RoIAlign 提取目标特征集合 $V_{target}$ 与背景特征集合 $V_{bg}$。采用主成分分析（PCA）投影至低维流形后，定义 Fisher 判别力分数（$S_{fce}$）作为衡量二元特征可分性的核心指标：

$$S_{fce}^{(l)} = \frac{\|\boldsymbol{\mu}_{target} - \boldsymbol{\mu}_{bg}\|_2^2}{\text{Tr}(\mathbf{\Sigma}_{target}) + \text{Tr}(\mathbf{\Sigma}_{bg}) + \epsilon}$$

其中，$\boldsymbol{\mu}_{target}$ 与 $\boldsymbol{\mu}_{bg}$ 分别为目标簇与背景簇的质心；$\text{Tr}(\mathbf{\Sigma})$ 表示协方差矩阵的迹。分子量化了目标信号与背景噪声的差异强度，分母量化了特征分布的发散程度。该指标刻画的是 Bottle 前景簇与水面背景簇的可分性。

**3.3.2 基于 FCE 的特征退化诊断**

[这里插入图 1：(a) FCE评分随深度的折线图 (b) 目标类内离散度直方图 (c) 前景-背景距离变化图 (d) 散点可视化流形图]

利用 FCE 探针对基准模型的逐层扫描揭示了以下病理特征：

1. **目标类内发散**：如图 1(b) 所示，Layer 19 表现出异常高的类内离散度，模型未能将破碎的瓶体局部特征聚类为一个紧凑的语义整体。
    
2. **前景-背景混叠**：如图 1(c) 所示，Layer 13 的类间分离度处于最低点，模型无法有效抑制与目标纹理高度相似的水波纹噪声。
    

**诊断结论**：Layer 13 至 Layer 19 是特征提取链路中的语义瓶颈。这归因于卷积网络缺乏长距离依赖建模能力，为在该层级引入具有全局感受野的 Mamba 提供了量化数据支撑。

#### 3.4 C3k2-Mamba：上下文感知的骨干重构

为解决深层特征语义塌陷问题，同时满足无人设备的实时性要求，本研究引入了选择性状态空间模型（Mamba），利用其线性复杂度进行长距离上下文扫描。

其核心离散化状态方程定义如下：

$$h_t = \bar{\mathbf{A}} h_{t-1} + \bar{\mathbf{B}} x_t$$

$$y_t = \mathbf{C} h_t$$

**针对性的深层骨干重构**

基于 FCE 的量化诊断结果，本文实施了局部架构的靶向重构。网络保留了浅层（P1-P3）的卷积范式以提取高频物理特征。针对出现严重语义混叠的深层（P4, P5），设计了 C3k2-Mamba 模块取代原生 Bottleneck。

该模块创新性地将 SSM 嵌入 CSP 架构，采用**双流密集聚合（Dual-stream Dense Aggregation）**机制：输入张量 $X$ 首先经 $1\times1$ 卷积降维，并沿通道拆分为守恒流 $X_{id}$ 与计算流 $X_{comp}$。计算流馈入密集连接的 Mamba Blocks 序列，输出序列 $Y_i$ 递归定义为：

$$Y_0 = X_{comp}, \quad Y_i = M_i(Y_{i-1}), \quad i=1, \dots, N$$

最终，输出由所有中间时序状态与守恒流拼接聚合：

$$Y_{out} = \text{Conv}_{1\times1}\left( \text{Concat}[X_{id}, Y_0, Y_1, \dots, Y_N] \right)$$

这种设计使模型能够在每一层将 Mamba 提取的全局水面上下文与局部目标特征进行精准对齐，将特定前景实体从复杂的背景流形中完美剥离。

---

### 第四章 实验与结果分析

#### 4.1 数据集与实验设置

**4.1.1 单一类别异构感知基准**

针对极具挑战性的透明及半透明塑料瓶，本研究构建了一个专注于单一类别的多源异构感知基准。将所有相关子类统一映射为唯一的检测类别：Bottle。数据集整合了包含偏振光学指纹的 PoTATO 子集、提供动态水波扰动的 FloW-Img 子集，以及自建的高难度极端反光数据集（Wild-Bottle 2400+）。

**4.1.2 实施细节**

所有实验均部署于搭载 Ubuntu 22.04 操作系统的 Linux 服务器上，硬件计算资源包含 NVIDIA RTX 5060 与 RTX 5070 GPU，基于 PyTorch 深度学习框架实现。本文选用 YOLO11n 作为基准模型并加载 COCO 预训练权重。输入分辨率统一为 640x640，批次大小设为 32。训练过程持续 200 个 Epochs，采用 SGD 优化器；在最后 10 个 Epoch 强制关闭 Mosaic 数据增强以微调 BatchNorm 统计量。改进后的最终模型统称为 YOLO11-MCN。

#### 4.2 主实验结果对比

为了评估 YOLO11-MCN 的性能，本研究将其与实时目标检测基准在统一实验协议下进行了对比。

[这里插入表 1：水面瓶体测试集检测性能对比大表，需加粗 YOLO11-MCN 的数据]

实验结果表明，YOLO11-MCN 在核心指标上实现了全面超越：

1. **单类精度跃升与召回率提高**：在 mAP@50 指标上，YOLO11-MCN 达到 95.86%，相较于基准提升了 8.05%，召回率同步攀升至 92.7%。这证明 Mamba 模块成功修复了深层特征的语义塌缩。
    
2. **定位能力增强**：在 mAP@50-95 指标上，改进模型提升至 65.19%。表明在水波干扰背景下，模型能生成更贴合透明目标轮廓的预测框。
    
3. **参数效率与实时性的极佳平衡**：YOLO11-MCN 的参数量压缩至 2.22 M（下降 15.3%）。在保持 7.8 GFLOPs 极低计算复杂度的同时，推理速度仍高达 125.8 FPS，完全满足端侧部署需求。
    

#### 4.3 核心消融实验

**4.3.1 物理模态的有效性验证**

[这里插入表 2：不同输入模态（RGB, DoLP, 全模态PDR）下的精度消融对比表]

在固定网络结构的前提下，仅采用 RGB 模态在强反光场景下表现欠佳；引入偏振模态（DoLP）后精度显著提升，证明偏振特征能有效增强漂浮物与水面背景的物理对比度；采用全模态随机训练策略（PDR）后模型达到最佳性能，验证了跨模态不变特征对抵御视觉伪装的有效性。

**4.3.2 基于 FCE 诊断的层级敏感性与结构有效性分析**

[这里插入表 3：Mamba模块在不同层级（P3, P4, P5）以及不同结构（纯Conv对比）下的消融对比表]

为了验证 FCE 诊断的准确性，本研究对比了 Mamba 模块的植入位置与结构。位置敏感性实验显示，将 Mamba 置于浅层（P3）性能提升微弱；而在 FCE 精准指出的语义瓶颈层（P4, P5）引入时，获得了最大的性能增益（mAP@50 提升 3.4%）。此外，与单纯堆叠卷积层相比，C3k2-Mamba 模块在更低的参数量下，展现出了更强的背景分离能力，形成了完美的逻辑闭环。

#### 4.4 可视化分析

[这里插入图 4：(左) 改进前后 Layer 19 的 FCE 散点对比图；(右) 改进前后网络 Grad-CAM 热力图对比]

**4.4.1 FCE 再评估与特征流形修复**

对训练后的 YOLO11-MCN 再次进行 FCE 分析，改进后的模型在 Layer 19 的特征分离度显著提升，数据点从塌陷区回归高判别力区域。

**4.4.2 语义聚焦热力图分析**

利用 Grad-CAM 技术提取的关注区域显示，基准模型的热力图常被发光水波扰乱导致误检；而 YOLO11-MCN 始终高度聚焦于漂浮物本体，直观证明了 Mamba 成功利用全局上下文抑制了背景一致性噪声。